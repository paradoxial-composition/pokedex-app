export type RootStackParamList = {
    Home: undefined; // No params for Home screen
    PokeList: undefined; // No params for PokeList screen
    // Add more screens as needed, e.g.:
    // Detail: { pokemonId: number };
  };
  